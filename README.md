# WebCacheSample_ASPNET_MVC

Uses Lazy and Memory cache to ensure single expensive operation.

> The actual operation here is not expensive. Its just a `DateTime.Now`. This repos shows one of the ways to use Lazy with MemoryCache.
